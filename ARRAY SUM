#include<stdio.h>
int main(){
        long long int a[10], sum=0, n, i;
    scanf("%lld", &n);
    
    for(i=0; i<n; i++){
        scanf("%lld", &a[i]);
        sum+=a[i];
    }
    printf("%lld", sum);
}
