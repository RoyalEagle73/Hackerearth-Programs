#include<stdio.h>
int main(){
    int T; scanf("%d", &T);
    
    int a[T];
    int i=0;
    for(i=0; i<T; i++)
    scanf("%d", &a[i])
    ;
    int n;
    scanf("%d", &n);
    
    for(i=0; i<T; i++){
        if(a[i]==n){
        printf("%d", i);
        break;
        }
    }
}
