#include<bits/stdc++.h>
using namespace std;


int main(){
    
    int num;
    cin>>num;
    
    num++;
    
    int date = 0;
    
    int hash[31] = {0};
    
    int weightage[100000] = {0};
    
    while(num--){
        
        string chat;
        getline(cin, chat);
        
        for(int i=0; i<chat.length(); i++){
            
            if( chat[i] >= '0' && chat[i] <= '9'){
                
                if( chat[i+1] >= '0' && chat[i+1] <= '9' ){
                    
                    date = 0;   
                    date = ( chat[i] - '0' )*10;
                    date += ( chat[i+1] - '0' );
                    i++;
                        
                }
                else{
                    
                    date += chat[i] - '0';
                    
                }
                
                if( chat[0] == 'G')
                    hash[ date ] += 2;
                else
                    hash[ date ]++;
                
              
            }
            
        }
        
     }
    
    int max = INT_MIN;
    int index;
    bool flag = false;
    
    for(int i=1; i<=30; i++){
        
        
        weightage[ hash[i] ]++;
        
        if( hash[i] > max){
            
            max = hash[i];
            index = i;
            
        }
        else if( weightage[ hash[i] ] > 1 && hash[i]!=0){
            flag = true;
            break;
        }
        
    }
    
    if( (index == 20 || index == 19) && flag == false )
        cout<<"Date ";
    else
        cout<<"No Date"<<endl;
    
}
